<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:redis="http://www.springframework.org/schema/redis"
       xsi:schemaLocation="http://www.springframework.org/schema/redis http://www.springframework.org/schema/data/mongo/spring-redis.xsd
          http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean name="quartzScheduler" lazy-init="false"
          class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="applicationContextSchedulerContextKey" value="applicationContextKey"/>
        <property name="configLocation" value="classpath:quartz.properties"/>
        <property name="triggers">
            <list>
                <ref bean="trigger"/>
            </list>
        </property>
    </bean>

    <bean id="trigger" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="jobDetail"/>
        <property name="cronExpression" value="0 0 0/1 * * ?"/>
    </bean>

    <bean id="jobDetail" class="org.springframework.scheduling.quartz.JobDetailBean">
        <property name="jobClass" value="com.xxx.ClassName"/>
    </bean>

<!--
    <bean id="baseQuartzScheduler" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        &lt;!&ndash;
        <property name="configLocation" value="classpath:quartz.properties" />
         &ndash;&gt;
        <property name="dataSource" ref="dataSource"/>
        <property name="transactionManager" ref="transManager"/>

        &lt;!&ndash; This name is persisted as SCHED_NAME in db. for local testing could change to unique name to avoid collision with dev server &ndash;&gt;
        <property name="schedulerName" value="quartzScheduler"/>

        &lt;!&ndash; Will update database cron triggers to what is in this jobs file on each deploy. Replaces all previous trigger and job data that was in the database. YMMV &ndash;&gt;
        <property name="overwriteExistingJobs" value="true"/>
        &lt;!&ndash;
        <property name="autoStartup" value="true" />
         &ndash;&gt;
        <property name="applicationContextSchedulerContextKey" value="applicationContext"/>
        <property name="jobFactory">
            <bean class="com.dennis.walking.api.web.schedule.AutowiringSpringBeanJobFactory"/>
        </property>
        &lt;!&ndash; NOTE: Must add both the jobDetail and trigger to the scheduler! &ndash;&gt;
        &lt;!&ndash;
        <property name="jobDetails">
          <list>
          </list>
        </property>
        <property name="triggers">
          <list>
          </list>
        </property>
        &ndash;&gt;
    </bean>

    <bean id="quartzScheduler" parent="baseQuartzScheduler">
        <property name="configLocation" value="classpath:quartz.properties"/>
        <property name="autoStartup" value="true"/>
        &lt;!&ndash; This name is persisted as SCHED_NAME in db. for local testing could change to unique name to avoid collision with dev server &ndash;&gt;
        <property name="schedulerName" value="apiQuartzScheduler"/>
        &lt;!&ndash; NOTE: Must add both the jobDetail and trigger to the scheduler! &ndash;&gt;
        <property name="triggers">
            <list>
                <ref bean="rollbackOrderStatusTrigger"/>
            </list>
        </property>

        <property name="jobDetails">
            <list>
                <ref bean="rollbackOrderStatus"/>
            </list>
        </property>
    </bean>-->
</beans>